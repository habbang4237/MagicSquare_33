# Magic Square — Project Rules (.mdc) 마이그레이션 보고서

| 항목 | 내용 |
|------|------|
| **문서 목적** | `.cursorrules` → `.cursor/rules/*.mdc` 분리·이전 및 README 반영 결과 정리 |
| **대상 독자** | SW 아키텍트, 프로젝트 이해관계자, TDD 실습 참여자 |
| **범위** | 본 세션(규칙 마이그레이션·문서화) 산출물 |
| **작성 기준일** | 2026-05-28 |
| **프로젝트** | MagicSquare_xx |
| **선행 문서** | `Report/03.CursorRules_UserEntity_구현보고서.md` |
| **대화 기록** | `Prompt/04.cursor_project_rules_mdc_migration_transcript.md` |

---

## 목차

1. [세션 요약](#1-세션-요약)
2. [산출물 목록](#2-산출물-목록)
3. [규칙 파일 매핑](#3-규칙-파일-매핑)
4. [frontmatter 적용 정책](#4-frontmatter-적용-정책)
5. [`.cursorrules` 처리](#5-cursorrules-처리)
6. [README 변경](#6-readme-변경)
7. [Report/03 이슈 대비](#7-report03-이슈-대비)
8. [다음 단계](#8-다음-단계)

---

## 1. 세션 요약

Report/03에서 **단일 `.cursorrules`(245행)** 로 완성했던 규칙을, Cursor 권장 방식인 **Project Rules(`.mdc`)** 로 분리·이전했다.

| 순서 | 사용자 요청 | 결과 |
|------|-------------|------|
| 1 | `.mdc` 5파일 구조로 전환 **방법만** 설명 | 섹션 매핑, `alwaysApply`/`globs`, 중복 제거·검증 절차 제안 |
| 2 | **진행** (마이그레이션 실행) | 5개 `.mdc` 생성, `.cursorrules`는 deprecated 안내로 축소 |
| 3 | `README.md` rule 관련 업데이트 | Project Rules 섹션·구조 트리·문서 이력 v1.1 |
| 4 | Report 작성 + Transcript Export | 본 보고서, `Prompt/04.*` |

---

## 2. 산출물 목록

### 2.1 Cursor Project Rules

| 파일 | 설명 |
|------|------|
| `.cursor/rules/magicsquare-project.mdc` | 프로젝트·계약·디렉터리·AI 작업 순서 |
| `.cursor/rules/magicsquare-forbidden.mdc` | 금지 패턴·대안 |
| `.cursor/rules/magicsquare-python-code-style.mdc` | Python 스타일 |
| `.cursor/rules/magicsquare-ecb-architecture.mdc` | ECB·Dual-Track |
| `.cursor/rules/magicsquare-tdd-testing.mdc` | TDD phase·pytest·커버리지 |

### 2.2 문서·기록

| 파일 | 설명 |
|------|------|
| `.cursorrules` | 7행 — `.cursor/rules/` 이전 안내 (deprecated) |
| `README.md` | Cursor AI 규칙 섹션, 구조·참고 링크 갱신 |
| `Report/04.ProjectRules_MDC_Migration_구현보고서.md` | 본 보고서 |
| `Prompt/04.cursor_project_rules_mdc_migration_transcript.md` | 본 세션 대화 Export |
| `scripts/export_transcript.py` | Transcript 경로·출력 인자 지원 (갱신) |

---

## 3. 규칙 파일 매핑

| `.cursorrules` 섹션 | 대상 `.mdc` |
|---------------------|-------------|
| `project`, `file_structure`, `ai_behavior` | `magicsquare-project.mdc` |
| `code_style` | `magicsquare-python-code-style.mdc` |
| `architecture` | `magicsquare-ecb-architecture.mdc` |
| `tdd_rules`, `testing` | `magicsquare-tdd-testing.mdc` |
| `forbidden` | `magicsquare-forbidden.mdc` |

**중복 정리 원칙:** ECB·TDD 상세는 각 전용 `.mdc`에 두고, `forbidden`·`project`는 요약·참조 위주.

---

## 4. frontmatter 적용 정책

| 규칙 파일 | `alwaysApply` | `globs` |
|-----------|---------------|---------|
| `magicsquare-project.mdc` | `true` | — |
| `magicsquare-forbidden.mdc` | `true` | — |
| `magicsquare-python-code-style.mdc` | `false` | `**/*.py` |
| `magicsquare-ecb-architecture.mdc` | `false` | `{src/magicsquare/**/*.py,tests/**/*.py}` |
| `magicsquare-tdd-testing.mdc` | `false` | `{tests/**/*.py,src/magicsquare/**/*.py}` |

---

## 5. `.cursorrules` 처리

| 항목 | 내용 |
|------|------|
| 이전 | 245행 YAML, 8섹션 전역 규칙 |
| 이후 | 5개 `.mdc`에 본문 이전 |
| 루트 파일 | deprecated 리다이렉트만 유지 (이중 적용 방지) |
| 삭제 | README에 “삭제 가능” 명시 — 팀 정책에 따라 선택 |

---

## 6. README 변경

| 변경 | 내용 |
|------|------|
| 프로젝트 구조 | `.cursor/rules/`, `Report/02`, `src/`, `tests/` 반영 |
| 신규 섹션 | **Cursor AI 규칙 (Project Rules)** — 5파일 표·확인 방법 |
| 참고 | Report/02, `.cursor/rules/` 링크 |
| 문서 이력 | v1.1 (rules 마이그레이션) |

---

## 7. Report/03 이슈 대비

| Report/03 이슈 | 본 세션 조치 |
|----------------|--------------|
| `.mdc` 마이그레이션 필요 | ✅ 5파일 분리 완료 |
| `.cursorrules` YAML 파싱 보장 없음 | ✅ `.mdc` frontmatter + Markdown 본문으로 전환 |
| `ai_behavior` pytest·커버리지 강제 한계 | ⏳ 열림 — CI·hook 권장은 동일 |
| `red_phase` import 오류 vs 첫 RED | ⏳ 열림 — `magicsquare-tdd-testing.mdc` 문구 조정 검토 |

---

## 8. 다음 단계

| 우선순위 | 작업 |
|----------|------|
| P0 | `Grid` + `MagicSquareValidator` Domain Track RED→GREEN |
| P0 | Boundary Input/Output 계약 (UI Track) |
| P1 | (선택) `.cursorrules` 파일 삭제, 팀 합의 후 |
| P1 | pre-commit / CI에서 pytest·커버리지 gate |
| P2 | 규칙·Report/02 Test-ID 동기화 자동 검사 |

---

## 변경 이력

| 버전 | 일자 | 변경 내용 |
|------|------|-----------|
| 1.0 | 2026-05-28 | Project Rules 마이그레이션 세션 보고서 최초 작성 |
